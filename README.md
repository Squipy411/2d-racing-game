# Relay Run

A self-contained 2D browser platformer. Open `index.html` in a browser to play.

Controls:

- Move: `A` / `D` or arrow keys
- Jump: `W`, up arrow, or space
- Dash: `Shift` or `K`
- Pause: `P` or `Esc`
- Restart: `R`

The game also includes touch buttons on small screens.
